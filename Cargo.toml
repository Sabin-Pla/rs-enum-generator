[package]
name = "enum-generator"
version = "0.1.0"
edition = "2021"

[lib]
name = "enum_generator"
path = "enum_generator.rs"

[dependencies]
enum-generator-macros = { path = "./enum-generator-macros", version = "0.1.0" }